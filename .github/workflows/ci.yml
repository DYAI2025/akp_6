name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality-and-deploy-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck, tests, build, and Railway smoke test
        run: npm run ci
